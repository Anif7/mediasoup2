{
  "name": "mediasoup-video-conference",
  "version": "1.0.0",
  "description": "Basic video conference using mediasoup SFU",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "esbuild public/client.js --bundle --outfile=public/bundle.js",
    "build:watch": "esbuild public/client.js --bundle --outfile=public/bundle.js --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mediasoup": "^3.11.26",
    "mediasoup-client": "^3.6.84",
    "express": "^4.18.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "esbuild": "^0.19.0"
  }
}
